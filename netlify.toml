[build]
  command = "npm run build"
  publish = "dist"

# Rewrite /api/tts to the Netlify serverless function for clean URLs
[[redirects]]
  from = "/api/tts"
  to = "/.netlify/functions/tts"
  status = 200
